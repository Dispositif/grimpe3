{% extends 'base.html.twig' %}

{% block body %}
    <h1>Sorties list</h1>

    <table>
        <thead>
            <tr>
                <th>Date</th>
                <th>Annonce</th>
                <th>min</th>
                <th>max</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for sortie in sorties %}
            <tr>
                <td><a href="{{ path('sortie_show', { 'idsortie': sortie.idsortie }) }}">{{ sortie.date|date('d-m H') }}h&nbsp;&nbsp;</a></td>
                
                <td>{{ sortie.annonce }}</td>
                <td>{{ sortie.niveaumin }}</td>
                <td>{{ sortie.niveaumax }}</td>
                
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('sortie_show', { 'idsortie': sortie.idsortie }) }}">voir</a>
                        </li>
                        <li>
                            <a href="{{ path('sortie_edit', { 'idsortie': sortie.idsortie }) }}">modifier</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('sortie_new') }}">Créer nouvelle sortie</a>
        </li>
    </ul>
{% endblock %}
